name: rebasemainforcepush
description: Fetch a branch, rebase current branch, then force push
examples:
  - p g rbfp
  - poulpe git rebasemainforcepush --branch=mainline
args:
  - name: --branch
    short: -b
    required: false
    help: Branch to rebase on
    default: main
